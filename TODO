* Add access to column values in model objects like in ActiveRecord, e.g. Node.path and Node.path= using method_missing.

* Refactor schema code to allow adapters to adapt schema definitions to specific capabilities.