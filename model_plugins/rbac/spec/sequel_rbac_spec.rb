require File.join(File.dirname(__FILE__), "spec_helper")

# it "should save a new object with the old data, and save the new data in a new object"
#...

describe Sequel::Plugins::Rbac do

  # Used to inject methods for converting text from one format to another
  # Takes a 'format', a 'raw' text and a 'formatted' column.
  # It then formats the raw text column, saving the result in either a 'formatted' column or the column specified.
  # The format it was saved in is stored in a 'format' column

end
